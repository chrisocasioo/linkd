# Assets

Place the following files here before building:

- `icon.png` — 1024×1024px, PNG, no transparency, no rounded corners. The Linkd gold blackletter logo on black background.
- `splash-icon.png` — Same image as icon.png (splash uses `resizeMode: "contain"` on `#0D0D0F` background).
